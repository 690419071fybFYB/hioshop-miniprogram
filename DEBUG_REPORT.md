# 小程序端错误分析与调试方案

## 1. 错误信息收集与分析

### 1.1 已观测错误
- ReferenceError: ApiRoot is not defined
- 触发位置：config/api.js 第 3 行，ApiRootUrl 计算时 ApiRoot 未定义
- 影响范围：所有 API 请求、页面初始化请求、登录逻辑

### 1.2 错误类型分类
- 逻辑错误：ApiRoot 未定义导致模块初始化失败
- 网络错误：API Root 未配置会引发请求失败或请求被拦截
- 渲染错误：API 依赖数据无法拉取导致页面空态或异常展示

### 1.3 触发场景与复现
- 场景：小程序启动或任意页面加载时引入 config/api.js
- 复现步骤：启动小程序，进入任意页面或触发网络请求
- 复现频率：100%

## 2. 系统性调试排查

### 2.1 基础库版本兼容性
- project.config.json 配置 libVersion 为 2.9.4
- 实际调试环境显示基础库 3.14.2
- 建议统一基础库版本或确保对差异 API 兼容

### 2.2 网络请求配置核查
- API Root 必须可访问，真机必须使用可公网访问的 HTTPS 域名
- 需在微信公众平台配置服务器域名白名单（request/upload/download）
- 本地调试可使用局域网 IP 并在开发者工具关闭域名校验

### 2.3 生命周期逻辑审查
- app.js onLaunch 内请求 AuthLoginByWeixin，依赖 ApiRoot
- 页面 onLoad/onShow 触发多次请求，需关注请求失败后的 UI 兜底

### 2.4 setData 与数据绑定风险
- 避免在高频回调中大量 setData
- onShow 内多次请求可合并或控制并发

### 2.5 组件通信与事件
- 当前项目以页面级调用为主，需确保组件 props 与事件名称一致

## 3. 代码质量检测

### 3.1 性能与内存
- 使用性能面板关注 setData 频率与渲染耗时
- 关注首页、分类、订单等列表页面的滚动渲染性能

### 3.2 代码检查
- 建议引入 ESLint 规则集并纳入 CI
- 排查未处理的 Promise 拒绝与 request 的 fail 分支

### 3.3 异步错误处理
- util.request 的 fail 分支已 reject，但调用侧需补充 catch
- 登录失败需提示并引导重试

### 3.4 本地存储
- 关注 wx.setStorage 的数据类型与大小限制
- 用户信息与 token 保存需有失效策略

## 4. 环境验证

### 4.1 设备矩阵
- iOS 与 Android 不同系统版本验证网络、登录与支付流程
- 不同机型验证图片与字体渲染

### 4.2 版本差异
- 开发版/体验版/正式版对比 API 域名与鉴权策略
- 体验版需绑定合法域名并验证 HTTPS 证书链

### 4.3 平台后台配置
- 服务器域名：request/upload/download 必须配置
- 业务域名：如涉及 web-view 需配置业务域名

## 5. 修复与验证

### 5.1 关键修复
- 配置 ApiRoot 以确保 API Root 有定义

### 5.2 单元与集成测试
- 登录流程：AuthLoginByWeixin 是否返回 token
- 订单与购物车：请求是否返回 200

### 5.3 回归测试
- 首页、分类、详情、购物车、下单、个人中心全链路回归

### 5.4 监控与上报
- 建议接入错误上报与接口失败告警

## 6. 预防与最佳实践

- 统一配置入口并对缺失配置做启动校验
- 对关键 API 做健康检查与可用性监控
- 增加开发、测试、生产三套配置与发布校验流程
